{
  "name": "ts-subpath-imports-exports-issue-reproduction",
  "version": "0.0.1",
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "start": "npm run -w shared -w server build && npm -w server start"
  }
}
